import React from 'react';
import {PathBar} from "./Paths.styles";

const Paths = () => {
    return (
        <PathBar>
            Главная {'>>'} Заглушка
        </PathBar>
    );
};

export default Paths;